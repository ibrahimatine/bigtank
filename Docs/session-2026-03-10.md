# Session du 10 mars 2026

## Resume

Grosse session : avancement Phase 3 (fonctionnalites manquantes) + Phase 4 (robustesse paiements) + Phase 5 (PM2) + Phase 6 (health checks).

## Ce qui a ete fait

### Phase 3 — Fonctionnalites

#### 1. Dashboard vendeur — Affichage expiration des annonces
- Ajout du champ `expiresAt` au type `ListingDetail` (frontend)
- Ajout des statuts `EXPIRED` et `DRAFT` dans les badges de `MyListingCard`
- Affichage "Expire dans Xj" avec icone horloge (rouge si < 7 jours)

#### 2. Limite de taille des images (5 Mo)
- **Frontend** (`image-upload.tsx`) : validation avant upload, rejet si > 5 Mo
- **Backend** (`image.service.ts`) : verification via `HeadObject` apres upload, suppression automatique si > 5 Mo

#### 3. Compression d'images cote client
- Redimensionnement max 1200px (garde les proportions)
- Conversion en JPEG qualite 85% via Canvas
- Applique dans `image-upload.tsx` (edition) et `listing-form.tsx` (creation)
- Dimensions reelles envoyees au backend au lieu de valeurs hardcodees

#### 4. Changement de mot de passe sur le profil
- Route proxy `POST /api/auth/change-password` (Next.js) — NOUVEAU
- Composant `ChangePasswordSection` dans `profile-form.tsx`
- Formulaire avec toggle visibilite, validation 8 caracteres

#### 5. Suppression du search-service vide
- Service vide (port 4006) supprime — Meilisearch gere directement par listing-service
- Retrait de `start-services.sh`

### Phase 4 — Robustesse paiements

#### 6. Timeout HTTP inter-services
- `AbortSignal.timeout(10000)` sur tous les appels notifications (auth, chat, listing, listing-cron)
- `AbortSignal.timeout(15000)` sur les appels Intech (payment-service) — plus long car API externe
- `AbortSignal.timeout(10000)` sur balance check et listing activate

#### 7. Idempotence webhooks
- Deja implementee ! Le webhook verifie `payment.status !== 'PENDING'` avant traitement
- Transaction Prisma atomique pour le changement de statut

### Phase 5 — Infrastructure

#### 8. ecosystem.config.js (PM2)
- Fichier PM2 pour tous les services (8 apps)
- max_memory_restart configure (200-500 Mo selon le service)
- Logs separes par service avec date format
- Pret a utiliser avec `pm2 start ecosystem.config.js`

### Phase 6 — Monitoring

#### 9. Health checks ameliores
- auth, listing, chat : verifient DB + Redis
- payment, notification, admin : verifient DB
- Retournent `status: 'degraded'` si un check echoue

## Build final
**11/11 services OK** en 34s

## Fichiers modifies/crees

### Nouveaux fichiers
- `apps/web/src/app/api/auth/change-password/route.ts`
- `ecosystem.config.js`

### Fichiers modifies
- `apps/web/src/lib/api.ts` — expiresAt
- `apps/web/src/components/dashboard/my-listing-card.tsx` — expiration + statuts
- `apps/web/src/components/dashboard/image-upload.tsx` — compression + limite 5 Mo
- `apps/web/src/components/dashboard/listing-form.tsx` — compression dans upload
- `apps/web/src/components/profile/profile-form.tsx` — changement mdp
- `apps/listing-service/src/image/image.service.ts` — verification taille backend
- `apps/auth-service/src/auth/auth.service.ts` — timeout
- `apps/chat-service/src/chat/chat.service.ts` — timeout
- `apps/listing-service/src/listing/listing.service.ts` — timeout
- `apps/listing-service/src/listing/listing-cron.service.ts` — timeout
- `apps/payment-service/src/payments/payments.service.ts` — timeout
- `apps/*/src/health.controller.ts` — health checks ameliores (6 services)
- `start-services.sh` — retrait search-service
- `Docs/PLAN-PRODUCTION.md` — mise a jour

### Supprime
- `apps/search-service/` — service vide supprime

### Phase 3 suite — Avatar + Meilisearch

#### 10. Upload photo de profil (avatar)
- `AvatarService` dans auth-service : presign S3 + confirm + suppression ancien avatar
- Endpoints `POST /auth/avatar/presign` et `POST /auth/avatar/confirm`
- Routes proxy Next.js creees
- Composant `AvatarUpload` dans profile-form.tsx : clic sur photo, upload S3, hover camera
- Limite 2 Mo, formats JPEG/PNG/WebP

#### 11. Verification sync Meilisearch
- Tous les cas couverts : create, update, status change, delete, activate after payment, CRON expiration, reindex apres images
- La recherche filtre par `status = ACTIVE` donc brouillons/expires n'apparaissent pas

### Fichiers supplementaires
- `apps/auth-service/src/user/avatar.service.ts` — NOUVEAU
- `apps/auth-service/src/user/user.module.ts` — ajout AvatarService
- `apps/auth-service/src/auth/auth.controller.ts` — endpoints avatar
- `apps/web/src/app/api/auth/avatar/presign/route.ts` — NOUVEAU proxy
- `apps/web/src/app/api/auth/avatar/confirm/route.ts` — NOUVEAU proxy

## Ce qu'il reste a faire (priorite)
- [ ] Tester le flux complet d'upload d'images avec MinIO
- [ ] Historique des ventes/achats
- [ ] Logging structure (Winston/Pino)
- [ ] Nginx + SSL
- [ ] Installer PM2 en production et migrer depuis nohup
