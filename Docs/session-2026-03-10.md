# Session du 10 mars 2026

## Resume

Avancement de la Phase 3 (Fonctionnalites manquantes) du plan de production.

## Ce qui a ete fait

### 1. Dashboard vendeur — Affichage expiration des annonces
- Ajout du champ `expiresAt` au type `ListingDetail` (frontend)
- Ajout des statuts `EXPIRED` et `DRAFT` dans les badges de `MyListingCard`
- Affichage "Expire dans Xj" avec icone horloge (rouge si < 7 jours)

### 2. Limite de taille des images (5 Mo)
- **Frontend** (`image-upload.tsx`) : validation avant upload, rejet si > 5 Mo
- **Backend** (`image.service.ts`) : verification via `HeadObject` apres upload, suppression automatique si > 5 Mo
- Texte d'aide mis a jour : "JPEG, PNG, WebP — Max 5 photos, 5 Mo/photo"

### 3. Changement de mot de passe sur le profil
- Creation de la route proxy `POST /api/auth/change-password` (Next.js)
- Ajout du composant `ChangePasswordSection` dans `profile-form.tsx`
- Formulaire avec : mot de passe actuel, nouveau, confirmation
- Toggle visibilite du mot de passe (icone oeil)
- Validation client : min 8 caracteres, correspondance des mots de passe

### 4. Suppression du search-service vide
- Le search-service (port 4006) n'avait qu'un health check — Meilisearch est appele directement depuis listing-service
- Suppression du dossier `apps/search-service/`
- Retrait de `start-services.sh` (lancement + kill + status check)
- Build 11/11 services OK

### 5. Mise a jour du plan de production
- Phase 3 bien avancee : forgot password, CRON expiration, dashboard expiration, limite images, changement mdp, suppression search-service — tous coches

## Fichiers modifies

- `apps/web/src/lib/api.ts` — ajout `expiresAt` au type
- `apps/web/src/components/dashboard/my-listing-card.tsx` — expiration + statuts EXPIRED/DRAFT
- `apps/web/src/components/dashboard/image-upload.tsx` — limite 5 Mo
- `apps/listing-service/src/image/image.service.ts` — verification taille backend
- `apps/web/src/app/api/auth/change-password/route.ts` — NOUVEAU proxy
- `apps/web/src/components/profile/profile-form.tsx` — section changement mdp
- `start-services.sh` — retrait search-service
- `Docs/PLAN-PRODUCTION.md` — mise a jour avancement
- `apps/search-service/` — SUPPRIME

## Ce qu'il reste en Phase 3
- [ ] Tester le flux complet d'upload d'images (presign → upload → confirm)
- [ ] Compression/redimensionnement d'images
- [ ] Verifier la sync Meilisearch
- [ ] Upload photo de profil (avatar)
- [ ] Historique des ventes/achats
