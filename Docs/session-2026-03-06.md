# Session du 06/03/2026 — Landing page, redirections, responsivite

## Modifications effectuees

### 1. Landing page — toutes les annonces avec pagination
- **`apps/web/src/components/home/recent-listings.tsx`**
  - Titre change : "Annonces recentes" → "Toutes les annonces"
  - Charge 20 annonces au lieu de 8 via `searchListings({ sortBy: 'date', limit: 20 })`
  - Ajout du composant `LoadMore` pour pagination infinie (bouton "Charger plus d'annonces")
  - Supprime le lien "Voir tout →" (plus necessaire)

### 2. Redirection post-login vers `/`
- **`apps/web/src/components/auth/login-form.tsx`** (ligne 46)
  - Apres connexion : redirige vers `/` au lieu de `/dashboard` (sauf ADMIN → `/admin`)
- **`apps/web/src/components/auth/register-form.tsx`** (ligne 62)
  - Apres inscription : redirige vers `/` au lieu de `/dashboard`
- **`apps/web/src/middleware.ts`** (lignes 33, 50)
  - User connecte sur `/login` ou `/register` → redirige vers `/` au lieu de `/dashboard`
  - Non-ADMIN qui tente `/admin` → redirige vers `/` au lieu de `/dashboard`

### 3. Responsivite
- **`apps/web/src/components/listing/listing-grid.tsx`**
  - Breakpoint `sm:grid-cols-3` → `md:grid-cols-3` (plus d'espace sur tablette)
  - Gap responsive : `gap-3 sm:gap-4`
- **`apps/web/src/components/listing/listing-card.tsx`**
  - Textes `text-[10px]` → `text-[11px]` (meilleure lisibilite mobile)
  - `truncate` sur le prix et `max-w-[60px]` + `truncate` sur la ville (pas de debordement)
- **`apps/web/src/components/home/hero.tsx`**
  - Padding : `py-12 sm:py-16` → `py-10 sm:py-12 lg:py-16`
  - Texte descriptif : `max-w-sm` → `max-w-xs sm:max-w-sm`
- **`apps/web/src/app/page.tsx`**
  - Section "Pourquoi BigTank" : `sm:grid-cols-3` → `md:grid-cols-2 lg:grid-cols-3`
- **`apps/web/src/components/search/load-more.tsx`**
  - Breakpoints et gap alignes avec listing-grid : `md:grid-cols-3 lg:grid-cols-4 gap-3 sm:gap-4`

## Verification
- `http://localhost:3000/` → toutes les annonces visibles + bouton "Charger plus"
- Se connecter → atterrissage sur `/`
- Chrome DevTools responsive : verifier 320px, 375px, 768px, 1024px, 1440px

## Erreurs TypeScript preexistantes (non liees)
- `src/app/dashboard/pay/[id]/page.tsx` : variable `refCommand` declaree mais non utilisee
